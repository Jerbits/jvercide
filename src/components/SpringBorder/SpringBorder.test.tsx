import React from 'react';
import { cleanup, render } from '@testing-library/react';
import { SpringBorder } from './SpringBorder'

afterEach(cleanup);

describe('SpringBorder component', () => {
    it('should', () => {
        // setup
        // const { getByText } = render(<SpringBorder  prop="prop" />);
        // test
        // expect(getByText('Button component prop')).toBeTruthy();
    });
})