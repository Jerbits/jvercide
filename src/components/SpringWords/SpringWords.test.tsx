import React from 'react';
import { cleanup, render } from '@testing-library/react';
import { SpringWords } from './SpringWords'

afterEach(cleanup);

describe('SpringWords component', () => {
    it('should', () => {
        // setup
        // const { getByText } = render(<SpringWords  prop="prop" />);
        // test
        // expect(getByText('Button component prop')).toBeTruthy();
    });
})